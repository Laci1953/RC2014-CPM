These files were used to build the executables

They were first obtained from the C source files, then modified to gain some space

The CGEN.INC contains the setting needed to choose for 64 / 128 KB RAM systems

Use makecgen.sub , then either link64.sub or link128.sub

Z80AS must be used as assembler

